syntax = "proto3";

service Message {
    rpc NormalMessage (MessageRequest) returns (MessageResponse);
    rpc SpecialMessage (MessageRequest) returns (MessageResponse);
}

message MessageRequest {
    string message = 1;
}

message MessageResponse {
    string message = 1;
}
